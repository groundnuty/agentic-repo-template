# Humanize prose

Run `/humanizer` once a draft is content-complete and before `/proofread` — style polish after the argument has settled, not during. It reports; you decide what to apply.

Domain caveat: technical terms are not AI-isms. "Examining" in a methods section, "underscores" in a discussion of variance — leave them. The full pattern list lives in the `humanizer` skill.
